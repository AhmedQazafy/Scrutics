# Scrutics Built-in ICS/OT Classification Rules
# ─────────────────────────────────────────────
# These are the factory rules that ship with the package.
# Do NOT edit this file — it may be overwritten on package updates.
# To override a rule or add custom ones, use custom_rules.yaml or
# drop a scrutics_rules.yaml in your working directory.
#
# Custom rules always take precedence over these.

rules:
  - name: "Modbus TCP"
    port: 502
    classify_as: "Modbus TCP"
    role: "PLC / RTU / Modbus Gateway"
    is_ot: true
    confidence: HIGH

  - name: "Siemens S7comm"
    port: 102
    classify_as: "S7comm"
    role: "Siemens PLC"
    is_ot: true
    confidence: HIGH

  - name: "EtherNet/IP"
    port: 44818
    classify_as: "EtherNet/IP"
    role: "Rockwell / Allen-Bradley Device"
    is_ot: true
    confidence: HIGH

  - name: "EtherNet/IP IO"
    port: 2222
    classify_as: "EtherNet/IP IO"
    role: "Rockwell IO Device"
    is_ot: true
    confidence: HIGH

  - name: "DNP3"
    port: 20000
    classify_as: "DNP3"
    role: "Utility RTU / IED"
    is_ot: true
    confidence: HIGH

  - name: "Niagara Fox"
    port: 1911
    classify_as: "Niagara Fox"
    role: "Building Automation Controller"
    is_ot: true
    confidence: HIGH

  - name: "OPC-UA"
    port: 4840
    classify_as: "OPC-UA"
    role: "OPC-UA Server / Gateway"
    is_ot: true
    confidence: HIGH

  - name: "OMRON FINS"
    port: 9600
    classify_as: "OMRON FINS"
    role: "Omron PLC"
    is_ot: true
    confidence: HIGH

  - name: "GE SRTP"
    port: 18245
    classify_as: "GE SRTP"
    role: "GE PLC"
    is_ot: true
    confidence: HIGH

  - name: "IEC 60870-5-104"
    port: 2404
    classify_as: "IEC 60870-5-104"
    role: "Power Grid RTU / IED"
    is_ot: true
    confidence: HIGH

  - name: "PCWorx"
    port: 1962
    classify_as: "PCWorx"
    role: "Phoenix Contact PLC"
    is_ot: true
    confidence: HIGH

  - name: "BACnet/IP"
    port: 47808
    classify_as: "BACnet/IP"
    role: "Building Automation Device"
    is_ot: true
    confidence: HIGH
